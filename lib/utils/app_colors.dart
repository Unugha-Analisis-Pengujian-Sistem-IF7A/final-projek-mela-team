import 'package:flutter/material.dart';

class AppColors {
  // Warna utama (utama + background)
  static const Color primary = Color(0xFF2A497D);    // biru tua tombol
  static const Color background = Color(0xFFE6F3FF); // biru muda pastel

  // Warna untuk dashboard_page.dart
  static const Color lightBlue = Color(0xFFDFF3F8); // background lembut
  static const Color darkBlue = Color(0xFF2C546A);  // header/top bar
  static const Color blue = Color(0xFF5AA9E6);      // card pemasukan
  static const Color pink = Color(0xFFF4B6C2);      // card pengeluaran
  static const Color green = Color(0xFF6BAA75);     // card sisa kas

  // Warna umum
  static const Color grey = Color(0xFFB0B0B0);
  static const Color textDark = Color(0xFF1F1F1F);
}
